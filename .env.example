# Server Configuration
PORT=3456
HOST=127.0.0.1

# Claude Configuration
# Specify alternate Claude config directory (optional)
# CLAUDE_CONFIG_DIR=~/.claude-inst7

# Default system prompt for all requests (optional)
# DEFAULT_SYSTEM_PROMPT="You are a helpful assistant."

# Context file name to read from working directory (default: CONTEXT.md)
# CONTEXT_FILENAME=CONTEXT.md

# CORS Configuration
# Set to 'false' to disable CORS (default: true)
# ENABLE_CORS=true

# Logging
# Options: debug, info, warn, error (default: info)
LOG_LEVEL=info

# Rate Limiting
# Maximum requests per minute (default: 60)
RATE_LIMIT_MAX=60

# Set to 'false' to disable rate limiting (default: true)
RATE_LIMIT_ENABLED=true

# Streaming Configuration
# Number of characters per streaming chunk (default: 20)
# Lower values = more frequent updates, higher values = fewer network writes
# STREAMING_CHUNK_SIZE=20

# ============================================================================
# INTELLIGENT GATEWAY CONFIGURATION
# ============================================================================

# Backend Registry
# Path to backends.json configuration file
BACKENDS_CONFIG=./config/backends.json

# SQLite Logging
# Path to SQLite database for request/response logging
DATABASE_PATH=./logs/requests.db

# Enable/disable SQLite logging (default: true)
ENABLE_SQLITE_LOGGING=true

# API Keys for Backend Providers
# These are referenced by authTokenEnv in backends.json
# Uncomment and set as needed:

# OpenRouter API key (for openrouter backends)
# OPENROUTER_API_KEY=sk-or-...

# Direct Anthropic API key (for anthropic-api backends)
# ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API key (for openai backends)
# OPENAI_API_KEY=sk-...

# Google API key (for gemini backends)
# GOOGLE_API_KEY=...

# Moonshot API key (for kimi backends via Claude CLI)
# MOONSHOT_API_KEY=sk-...
